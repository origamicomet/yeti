application = {
  window = {
    title = "engine";
    width  = 640;
    height = 480;
  };

  graphics = {
    adapter = 0;

    resolution = {
      width  = 640;
      height = 480;
    };

    fullscreen = false;
    vertical_sync = true;
  };

  win32 = {
    save_directory = "%APPDATA%/lwe/saves";
    time_step_policy = { type = "variable"; };
  };

  boot_package = "boot.package";
  boot_script = "boot.script";
};