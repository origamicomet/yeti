/* All we're doing is providing a compilation unit for the single file
   library. */
#define XML_IMPLEMENTATION

/* Include implementation. */
#include "yeti/core/misc/xml.h"

/* Undefine to prevent duplication implementation when using unity builds. */
#undef XML_IMPLEMENTATION
